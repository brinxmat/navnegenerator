import PropTypes from 'prop-types'

const AppProptypes = {
  name: {
    includeLast: PropTypes.bool,
    firstGender: PropTypes.string,
    firstDouble: PropTypes.bool,
    lastDouble: PropTypes.bool
  }
}

export default AppProptypes
